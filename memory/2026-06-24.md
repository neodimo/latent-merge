# 2026-06-24

## Morning photographic fixture intake gate

Delta: issue #3 still has no plate-sourcing decision, and the repo had no
fail-closed validator for accepting the next photographic fixture.

Action completed: added `scripts/validate_photographic_fixtures.py` plus tests.
The validator checks provenance, source/license/tonemap metadata, canonical
plate/CG/alpha files, dimensions, non-degenerate and matching alpha, recorded
hashes, and a configurable minimum fixture count.

Evidence:

```bash
.venv/bin/python -m unittest \
  tests.test_validate_photographic_fixtures \
  tests.test_phase2_rejection_checks
```

Result: 5 tests OK.

```bash
PYTHONPATH=. .venv/bin/python scripts/validate_photographic_fixtures.py \
  --min-count 5
```

Result: the existing `compositingpro_sh009_minimal` fixture passes its intake
contract at 1920x1080, while the quality gate correctly remains closed at 1/5
photographic fixtures. The generated JSON evidence was kept as deliberate
scratch at `/tmp/latent-merge-photographic-fixture-gate-20260624.json`.

Today's gate: reach five validator-clean photographic fixtures. Most valuable
DiMo action remains either supplying real use-case plates/footage or approving
CC0/free plate and matched-HDRI sourcing on issue #3.

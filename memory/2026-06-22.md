# 2026-06-22

## Morning IC-Light PCI inventory check

Morning cron action: extended the IC-Light runtime preflight to record `lspci`
GPU inventory so the CUDA blocker can distinguish "driver/device nodes not
passed through" from "no NVIDIA hardware visible in this environment."

Evidence run:

```bash
PYTHONPATH=. python3 scripts/check_ic_light_runtime.py --require-cuda \
  --json-out reports/ic-light-runtime-check-20260622-morning.json
```

Result: SD1.5 IC-Light weights remain valid (`fbc` 12-channel, `fc`
8-channel), but `ready=false`; no `/dev/nvidia*`, no
`/proc/driver/nvidia/version`, `torch.cuda.is_available=false`, and
`nvidia-smi` cannot communicate with the driver. New PCI inventory shows only
AMD Strix Halo display hardware and no NVIDIA PCI device.

Current concrete gate: run this same preflight on the actual 3080 Ti host or
route this project worker onto a process/environment where the NVIDIA PCI
device and `/dev/nvidia*` nodes are visible, then run the first IC-Light output
against `fixtures/compositingpro_sh009_minimal`.

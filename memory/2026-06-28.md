# 2026-06-28

## Afternoon

Delta vs last Gonzo pulse: commit `3f1cae9` landed this morning and fixed the
matched-HDRI CG renderer to output at the source plate resolution instead of the
old implicit 512x512 render size. That matters because `assemble_fixture.py
--cg` requires full-plate-size RGBA input, and the 06-27 proof fixture was
manually/scratch aligned around that constraint.

Action: did not rerender another demo or pad issue #3. Verified the fresh render
resolution fix plus the fixture assembly/validator path with:

```bash
PYTHONPATH=. .venv/bin/python -m unittest \
  tests.test_render_cg_insert \
  tests.test_assemble_fixture \
  tests.test_validate_photographic_fixtures
```

Evidence: 12 tests OK. No generated artifacts. No new scratch files.

Implication: the technical state is clean enough for the next run to be
decision-driven, not demo-driven. Gate remains 1/5 counted photographic
fixtures, with the toolchain ready for either path:

- YES on CC0 pano crops counting as real photographic plates -> produce and
  validate the 5-case intake set.
- NO -> wait for DiMo real footage/plates plus matched HDRI.

Next owner: DiMo for the binary L1 ruling. Gonzo owns immediate intake
production/validation only after that ruling or after DiMo supplies plates.

# 2026-06-23

## Morning fixture gate re-scope

Morning cron delta: `main` advanced since the last Gonzo pulse to
`5cef0dd docs: document Phase 1 PCT-Net baseline`. No open PRs are active.
GitHub issue #3 was still framed around the old Blender-mediated fixture
review, which is stale after the 2026-06-17 reset because those cases are
smoke-only, not quality-gate evidence.

Action completed: re-titled issue #3 to "Fixture: build locked photographic
eval set" and added a 2026-06-23 re-scope comment naming the real gate:
freeze 5-8 photographic plate+CG+alpha cases like `compositingpro_sh009_minimal`
before Layer 2 can honestly start.

Evidence:

```bash
.venv/bin/python -m unittest \
  tests.test_phase2_rejection_checks \
  tests.test_ic_light_runtime_check \
  tests.test_ic_flux_model_status
```

Result: 5 tests OK, 2 skipped for optional deps.

```bash
PYTHONPATH=. python3 scripts/check_ic_light_runtime.py --require-cuda \
  --json-out reports/ic-light-runtime-check-20260623-morning.json
```

Result: IC-Light SD1.5 weights remain valid (`fbc` 12-channel, `fc`
8-channel), but CUDA remains unavailable in this process: no `/dev/nvidia*`,
no NVIDIA PCI device, `torch.cuda.is_available=false`.

Current concrete gate: DiMo either drops real use-case plates/footage or
approves Gonzo sourcing CC0/free photographic backplates + matched HDRIs for
the remaining eval fixtures.

## Afternoon release commitment re-scope

Delta: no code, PR, fixture, or runtime capability changed after the morning
pulse. GitHub issue #6 still required a runnable artifact at every nominal
phase, which conflicted with the 2026-06-17 reset: packaging a synthetic or
Blender-mediated milestone would preserve invalid quality evidence.

Action completed: re-scoped issue #6 to trigger only after the first accepted
photographic Layer-2 gate. Its prerequisites now explicitly depend on issue #3,
an actual raw-vs-harmonizer/relight comparison over untouched plates, and a
human preference decision. Removed the paused Bert lane and named Gonzo as
packaging owner after DiMo acceptance.

Evidence: <https://github.com/neodimo/latent-merge/issues/6> and re-scope
comment <https://github.com/neodimo/latent-merge/issues/6#issuecomment-4784204899>.

Next run target: check issue #3 for a plate-sourcing decision. If approved,
start one photographic fixture; if real plates arrive, validate and stamp the
first intake case. Do not spend another pulse on release packaging before that
evidence exists.

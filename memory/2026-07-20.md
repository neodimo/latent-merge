# 2026-07-20

## Morning smoke-fixture generator alignment

Delta vs the 2026-07-19 afternoon pulse: issue #3 and issue #6 remain
unchanged, there is no active PR, and `main` only advanced with yesterday's
Phase 0 documentation cleanup.

Bottleneck: DiMo's issue #3 L1 ruling still blocks photographic fixture intake.

Action completed: finished the executable-source half of yesterday's cleanup.
`scripts/smoke_pipeline.py` and the committed golden fixture manifest now call
the synthetic case a deterministic regression sentinel excluded from
photographic quality evaluation. The generator also emits
`plate_provenance: synthetic`, preserving the fixture-law stamp on regeneration.

Verification: regenerated the fixture and smoke run into `/tmp`, parsed and
asserted the generated manifest, and confirmed SHA-256 equality for all three
generated images against the committed golden fixture. `git diff --check`
passes. The temporary evidence is intentionally scratch and outside the repo;
there is no generated visual artifact to retain.

Today's concrete gate remains DiMo's issue #3 YES/NO ruling. The most valuable
DiMo action is answering that ruling on GitHub. Next owner: DiMo for the ruling;
Gonzo for intake after YES or supplied plates/HDRI after NO.

## Afternoon automation-status cleanup

Delta vs the morning pulse: issue #3 and issue #6 remain unchanged, there is no
active PR, and `main` only advanced with the morning smoke-fixture provenance
fix.

Bottleneck: DiMo's issue #3 L1 ruling still blocks photographic fixture intake.

Action completed: corrected the communication example in `AUTOMATION.md`. It
still described the current transform as a stub and directed the next action
toward replacing it, despite the shipped PCT-Net baseline and the photographic
evidence gate. The replacement demonstrates the required delta, evidence,
implication, and named-owner structure without claiming to be current state,
and the document now tells workers to replace every example fact.

Verification: targeted stale-phrase checks return no match; `git diff --check`
passes. No generated artifacts were created. Next run target: inspect issue #3
first and act only on new input or a newly verified stale project-facing
commitment.

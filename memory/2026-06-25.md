# 2026-06-25

## Morning unique photographic fixture gate

Delta: the photographic intake validator counted directories, so copied
fixtures sharing the same plate could incorrectly satisfy the five-case gate.

Action completed: changed the root validator to count unique `plate_rgb.png`
content hashes, reject duplicate photographic plates with the affected fixture
paths, and report both discovered and unique fixture counts.

Evidence:

```bash
.venv/bin/python -m unittest \
  tests.test_validate_photographic_fixtures \
  tests.test_phase2_rejection_checks
```

Result: 7 tests OK.

```bash
PYTHONPATH=. .venv/bin/python scripts/validate_photographic_fixtures.py \
  --min-count 5 \
  --json-out /tmp/latent-merge-photographic-fixture-gate-20260625-morning.json
```

Result: the live gate correctly remains closed at 1/5 unique photographic
fixtures. The JSON report is deliberate local scratch in `/tmp`.

Today's gate: five validator-clean, unique photographic plates. Most valuable
DiMo action remains supplying real use-case plates/footage or approving
CC0/free photographic plate plus matched-HDRI sourcing on issue #3.

## Afternoon fixture commitment re-scope

Delta: no new fixture, PR, or runtime capability landed after the morning
pulse. GitHub issue #3 had been retitled for the photographic eval set, but its
body and overdue Phase 1 milestone still described the superseded single-case
pipeline task.

Action completed: removed the stale Phase 1 milestone and rewrote issue #3's
acceptance contract as a staged photographic gate: five validator-clean,
unique plates unlock backend comparison; the formal Phase 2 pass still
requires the locked 10–20-case set plus a sequence. The issue now names the
canonical files, metadata, pristine-plate rule, uniqueness rule, validator
command, current 1/5 evidence, and owner handoff.

Evidence: <https://github.com/neodimo/latent-merge/issues/3>. Targeted fixture
and rejection tests still pass, and the live validator remains correctly
closed at 1/5 unique photographic plates. The JSON report is deliberate local
scratch in `/tmp/latent-merge-photographic-fixture-gate-20260625-afternoon.json`.

Next run target: only intake evidence can advance the project. If DiMo supplies
plates/footage or approves sourcing, Gonzo validates and integrates fixture #2;
otherwise do not recycle another unchanged runtime or gate-status pulse.

# 2026-06-21

## Morning IC-Light runtime preflight

Morning cron action: re-ran the SD1.5 IC-Light runtime preflight to check
whether the 3080 Ti/CUDA visibility blocker had cleared in this run.

Evidence run:

```bash
PYTHONPATH=. python3 scripts/check_ic_light_runtime.py --require-cuda \
  --json-out reports/ic-light-runtime-check-20260621-morning.json
```

Result: `weights_ready=true` for the symlinked SD1.5 IC-Light FBC/FC weights
(`fbc` 12-channel, `fc` 8-channel), but `cuda_ready=false`;
`torch.cuda.is_available=false`, `device_count=0`, and `nvidia-smi` still could
not communicate with the NVIDIA driver. No relight output was produced.

Current concrete gate: get this same preflight to `ready=true`, then run the
first real IC-Light output against `fixtures/compositingpro_sh009_minimal`.

## Afternoon IC-Light host CUDA diagnosis

Afternoon cron action: extended the IC-Light runtime preflight so future runs
record host CUDA visibility in addition to weight shape, PyTorch CUDA, and
`nvidia-smi`.

Evidence run:

```bash
PYTHONPATH=. python3 scripts/check_ic_light_runtime.py --require-cuda \
  --json-out reports/ic-light-runtime-check-20260621-afternoon.json
```

Result: weights still pass (`fbc` 12-channel, `fc` 8-channel) and `libcuda` /
NVML libraries resolve, but this process sees no `/dev/nvidia*` device nodes and
no `/proc/driver/nvidia/version`. `torch.cuda.is_available=false` and
`nvidia-smi` still cannot communicate with the driver. This re-scopes the next
action to host/device exposure before more IC-Light runner work.

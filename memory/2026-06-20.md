# 2026-06-20

## IC-Light runtime preflight

Night cron action: added `scripts/check_ic_light_runtime.py` and
`tests/test_ic_light_runtime_check.py` so the project can distinguish missing
or incompatible SD1.5 IC-Light weights from CUDA runtime visibility.

Evidence run:

```bash
PYTHONPATH=. python3 scripts/check_ic_light_runtime.py --require-cuda \
  --json-out reports/ic-light-runtime-check-20260620.json
```

Result: `weights_ready=true` for the symlinked SD1.5 IC-Light FBC/FC weights
(`fbc` 12-channel, `fc` 8-channel), but `cuda_ready=false`; this cron scope
reported `torch.cuda.is_available=false` and `nvidia-smi` could not communicate
with the NVIDIA driver. No fresh relight output was produced.

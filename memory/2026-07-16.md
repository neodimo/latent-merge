# 2026-07-16

## Morning smoke-tooling wording cleanup

Delta vs last Gonzo pulse: issue #3 still has no DiMo YES/NO ruling after the
2026-06-30 re-scope, issue #6 is unchanged, and there are no active PRs since
the 2026-07-15 afternoon pulse.

Bottleneck: the project remains blocked on DiMo's issue #3 L1 ruling about
whether CC0 panorama-derived rectilinear crops count as photographic plates.

Action completed: closed a remaining smoke-tooling wording contradiction.
`scripts/validate_smoke_blender_fixtures.py` no longer describes its PASS as
validating photographic Blender intake fixtures, and
`scripts/create_smoke_blender_fixtures.py` now regenerates the smoke manifest
with the same smoke-only purpose already committed in
`fixtures/smoke_blender_set/manifest.json`.

Verification: inspected issue #3, issue #6, recent PR state, `LOCKED.md`,
`NEXT_STEPS.md`, `WORKFLOW.md`, `PHASE2_GATE.md`, and recent project memory.
Ran targeted `rg` checks for stale smoke/real-plate wording in the affected
scripts, then ran:

```bash
blender --background --python scripts/validate_smoke_blender_fixtures.py
```

Result: Blender 5.1.2 loaded both committed smoke scenes and reported
`PASS: validated 2 smoke-only Blender fixtures`.

Implication: the smoke Blender validator remains useful as a plumbing
regression check without implying its fixtures satisfy photographic quality
intake. Today's concrete gate remains DiMo's issue #3 YES/NO ruling. Most
valuable DiMo review: answer that ruling on GitHub. Next owner: DiMo for the
ruling; Gonzo owns fixture intake immediately after YES, or waits for supplied
plates/HDRI after NO.

## Afternoon IC-Light runner gate wording cleanup

Delta vs last Gonzo pulse: issue #3 still has no DiMo YES/NO ruling after the
2026-06-30 re-scope, issue #6 is unchanged, and there are no active PRs since
the morning pulse.

Bottleneck: the project remains blocked on DiMo's issue #3 L1 ruling about
whether CC0 panorama-derived rectilinear crops count as photographic plates.

Action completed: closed a remaining IC-Light/Flux runner contradiction.
`scripts/ic_flux_runner.py` no longer says Phase 2 evidence depends on Blender
target-reference cases passing. It now matches `docs/IC_LIGHT_FLUX_STATUS.md`
and `PHASE2_GATE.md`: accepted photographic fixtures, Layer-1 pass, and blind
Layer-2 preference over raw A-over-B.

Verification: inspected issue #3, issue #6, recent PR state, `LOCKED.md`,
`NEXT_STEPS.md`, `WORKFLOW.md`, `AUTOMATION.md`, `PHASE2_GATE.md`,
`docs/IC_LIGHT_FLUX_STATUS.md`, and recent project memory. Ran targeted `rg`
checks for stale Blender target-reference quality-gate wording; the remaining
matches are historical notes or the fixed runner policy. No generated visual or
run artifact was created.

Implication: the IC-Light runner's embedded policy no longer points future
workers back to smoke/Blender quality evidence. Next owner: DiMo for the issue
#3 ruling; Gonzo owns fixture intake immediately after YES, or waits for
supplied plates/HDRI after NO.
